services:
  mutex:
    image: smeans/mutex.us
    command:
      - /mutex-server
      - -addr
      - 0.0.0.0:8080
      - -dbPath
      - /data/mutex_site.db
      - -purgeInterval
      - 120m
    volumes:
      - ./data:/data
    ports:
      - "8088:8080"
    restart: on-failure